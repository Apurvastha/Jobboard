[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'gunicorn jobboard.wsgi:application --bind 0.0.0.0:$PORT --workers 2'"
healthcheckPath = "/api/v1/jobs/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.releaseCommands]]
command = "python manage.py collectstatic --noinput && python manage.py migrate"