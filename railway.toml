[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'celery -A jobboard worker --loglevel=info & gunicorn jobboard.wsgi:application --bind 0.0.0.0:$PORT --workers 2'"
healthcheckPath = "/health/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.releaseCommands]]
command = "python manage.py collectstatic --noinput && python manage.py migrate"